# Observable structural perturbation audit — 20 pairs

Construction: `swap D2/D3 child content in the tree and swap serialized Problem/Theory order in embedding input; labels are controlled negatives`. All text inputs changed: `True`.

SW-BTED structural-only accuracy at threshold `0.45`: **85.0%**; mean similarity `0.4339`.

MiniLM full-text accuracy at the same threshold: **0.0%**; mean cosine `0.9063`.

This is a secondary controlled diagnostic with constructed negative labels, not a replacement for the canonical real-only benchmark.